//! Discussion command implementations.
//!
//! Thread/message SQL helpers and `send_chat` generation stay in `lib.rs`
//! until they can move together with `DiscussionGeneration` without changing
//! DesktopClient command names.
