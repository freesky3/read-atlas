Repair the previous Figure Lens into the exact strict schema in {output_language}. Correct only schema, completeness, and evidence-whitelist violations. If the takeaway, a section body, or a panel/hotspot list uses literal `\\n` or `；` to enumerate points, reformat each point as its own CommonMark list item (`1. ` / `- `). Do not add ungrounded claims. This is the single allowed repair attempt.
