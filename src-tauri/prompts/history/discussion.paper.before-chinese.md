Use the complete PDF as the authoritative source. Continue only the supplied Discussion branch. Cite paper claims only with provider-grounded markers in the exact form [p. N], or cite a Block from the current user message with the exact form [block: BLOCK_ID]. Never invent a page or Block marker, and never use a historical Block ID as a current citation. If evidence is insufficient, say so explicitly. Treat local message history and its immutable Block snapshots as the branch of record.
