# Windows Alpha 发布检查表

本页记录发布所需的证据。准备完成、自动化通过和人工实机通过是不同状态。当前首发安装验收见 [Read Atlas Windows 生命周期验收](windows-install-validation.md)。早期准备记录见 [开源准备](release-validation-2026-09-11.md)。

## 源码公开

- [x] MIT、第三方声明与分发资源已检查。
- [x] 当前候选与完整可达 Git 历史通过 Gitleaks；未发现真实凭据，已核对唯一测试假值例外。
- [x] 本地发布候选包含运行必需的新代码、提示词和文档，排除用户数据和个人制作文件。
- [x] 独立干净源码快照通过 `npm ci`、仓库检查、前端测试／构建和 Rust 测试。
- [x] npm 与 Cargo 漏洞结果已处理，任何例外有范围、原因及期限。
- [ ] GitHub 仓库已设置远端，启用 private vulnerability reporting 和 Dependabot。
- [ ] 主分支要求 CI 通过；工作流默认最小权限；不向 fork PR 提供凭据。
- [x] README 中的平台、模型和版本支持范围与实际验证一致。

角色与素材授权未纳入本轮修改；不要将 MIT 采用解释为这部分已经取得新授权。

## 安装包

- [x] 当前发布候选生成 NSIS 和 MSI，并保存 SHA-256（最终远端提交仍需 CI 重建）。
- [ ] 在干净 Windows x64 机器安装；缺少 WebView2 时行为明确。
- [x] 已验证 NSIS 安装版的启动、正常退出、工作区选择、导入和 PDF 渲染。
- [x] 内部 0.1.0 → 0.1.1 升级：PDF、标签、批注、讨论、读者背景和阅读状态保留；其他历史版本／生成成果迁移另行验证。
- [x] NSIS 卸载保留整个文库，重新安装后再次打开并核对通过。
- [x] 已记录两个候选包未签名；签名证书配置与签名包验证仍待完成。
- [x] 安装资源含 LICENSE 与第三方声明；浏览器验证无远程字体请求（原生安装环境待验收）。
- [ ] Release 标为 prerelease，附支持范围、变化、限制、安装与校验说明。

Windows installer workflow 只生成 artifacts，不自动创建公开 Release。二进制发布前再次核对依赖和材料分发范围。

## 数据安全与真实服务商

- [x] 完整合成文库备份在独立目录恢复，PDF、批注、讨论与阅读状态可读取。
- [ ] 外部改名／移动、重复导入、回收站与跨工作区切换验收。
- [ ] 请求中断／超时／429／取消与恢复不造成不明确的重复付费。
- [ ] 测试 Gemini、Mistral 以及实际计划声明支持的其他服务商。
- [ ] 远端文件复用、删除失败恢复和实际 TTL／保留策略记录。
- [ ] 地图和旁批以真实论文核对引用、事实、布局与阅读干扰。
- [x] 共用生成确认页的中英提示通过浏览器检查（原生桌面待验收）。

真实调用只能使用专用样本、明确选择的服务商与测试凭据。PR CI 不读取开发者的系统凭据，不执行收费模型调用。

## 性能与兼容性

记录 Windows 版本、CPU、内存、应用版本／提交、PDF hash、样本数量和测量方法。P95 必须来自多次实测，不能用一次启动或构建时长代替。

- [ ] born-digital、扫描、图表／公式、500 页和损坏 PDF。
- [ ] 500 文件导入、Explorer 拖入和外部多事件变更。
- [ ] 冷启动、空闲与单 PDF 内存、Reader 交互延迟、安装包体积。
- [ ] 离线打开、系统字体回退、缩放／主题与较小窗口。
- [ ] 已知性能目标与实际结果分别列出，未测数据标为未测。

## 发布记录模板

| 项目 | 环境／样本 | 结果 | 证据 |
| --- | --- | --- | --- |
| 提交与工具链 | commit、Node、Rust、Windows | 待填写 | CI run / 本地记录 |
| 自动化 | 前端／Rust／依赖／密钥 | 待填写 | 脱敏摘要 |
| 安装与升级 | 安装包 SHA-256、来源版本 | 待填写 | 人工记录 |
| 实际模型 | 提供方、模型、日期、样本 hash | 待填写 | 脱敏记录 |
| 性能 | 方法、样本数、硬件 | 待填写 | 测量记录 |
