# 人物旁批第二轮审查与修复

日期：2026-09-10。基准：上轮 [审查报告](reading-guide-characters-review.md)、[执行计划](reading-guide-characters-plan.md) 与当前未提交工作树。用户授权复审并直接修复，随后授权继续列明的冻结计划与恢复接入。

结论：上轮十项主问题及本轮发现的运行时阻断已修复并接入。最终自动化验证：**506 项 Rust 库测试、34 项前端测试通过，生产构建成功**。P7 的真实模型内容质量和桌面视觉验收未进行；没有发起真实付费模型调用。

## Standards

| 问题 | 本轮实际处理 | 主要入口 |
| --- | --- | --- |
| S1：头像存储与展示路径不稳 | 使用受管相对路径与不可变内容摘要副本，读取拒绝路径穿越、软链和摘要不匹配；工作区移动、头像移除和替换均有回归 | `guide_character_assets.rs`、`lib.rs::get_guide_character_avatar`、`src/guide/personas.ts` |
| S2：旧提示词历史来源丢失 | 保留旧自定义稿的 V1 来源；补齐 known＋known 出厂稿迁移提前返回时漏记 previous_text 的分支 | `prompt_settings.rs::migrate_guide_pair` |
| S3：中断或并发保存重置人物 | 文件锁覆盖读取、修订检查及写入；完整 pending store 在恢复时重放，旧文件备份不被出厂配置掩盖 | `guide_character_settings.rs` |
| 本轮新发现：保存会删除非角色用户目录 | 移除无差别递归删除；退役 ID 隐藏角色，保留人物目录和用户资料 | `guide_character_settings.rs` |
| 全量回归发现：第一次重开 v8 会补表写盘 | 新建及旧版迁移时建立旁批扩展表，已完整的 v8 重开只执行幂等检查；字节不变回归已通过 | `v2_workspace.rs` |

这是实际数据行为修复；未为了代码风格改写无关模块。

## Spec

| 上轮问题 | 当前行为与验证 |
| --- | --- |
| F1：V2 未贯穿实际执行 | 实际 worker 使用 V2 memo 与批次核心。模型响应走动态人物和锚点白名单；已保存 `anchor` 结构由独立恢复器读取，ID、人物和回复关系不再丢失。模型仍不能提供 bbox/anchor。 |
| F2：材料、memo 和密度仍沿用旧逻辑 | 计划冻结完整分片材料，保留父块与邻接 context；worker 直接使用冻结材料，不再按父块取回未分片全文。相关 memo、错误原文、已接受锚点摘要分别进入初次／修复／补充。实际请求检查输入预算，逐页报告文字覆盖与缺口。 |
| F3：计划并未冻结 | 同一次提示词读取确定协议和全文；冻结角色、Reader、PDF/OCR、批次、语言及预算。返回 planId/digest，开始时校验当前输入、精确路由和模型；变化须重新计划。根调用单列并计入确认面板估计。 |
| F4：付费结果和发布无法可靠恢复 | 每个逻辑槽记录 in-flight 与完整 response/receipt，解析前落盘；恢复重放已收结果，未知结果不自动重发。memo 一次修复，批次一次结构修复与一次覆盖补充；收据按任务、逻辑槽和 route 去重。发布事务记录稳定 publishId，旧任务恢复不覆盖新 head。 |
| F5：阵容串文档、取消也写偏好 | 面板草稿按文档/修订隔离，过期异步响应不覆盖新计划；文档偏好优先于工作区默认；取消不保存，成功入队后记录实际阵容。启动命令避免同一文档跨路由并发生成。 |
| F6：未保存设置草稿丢失 | 默认/预设阵容变化保留人设草稿；鼠标、键盘、Esc、返回、导航和关闭执行一致保护；从角色设置返回时重新规划。 |
| F7：没有真实试写入口 | 显式按钮发送当前未保存草稿，与最多两位其他人物使用同一虚构材料比较；提供取消、真实用量、快照署名和主动样稿采纳。打开设置、静态预览、编辑本身不调用模型。 |

此外完成了预设阵容选择、可调头像裁剪、严格 memo 必需数组与嵌套字段/定位检查、提示词输入材料指令边界，以及教材 Markdown/数学转义说明。四份运行时稿与定稿文档的一致性由测试校验，保留完整中文说明。

## 验证证据

| 检查 | 结果 |
| --- | --- |
| `cargo test --manifest-path src-tauri/Cargo.toml --lib --no-fail-fast` | **506 passed / 0 failed**；`guide-review-full-rust-tests.log` |
| 旁批、角色设置与 SettingsWorkbench 前端回归，`--maxWorkers=1` | **9 个文件，34 passed / 0 failed**；`guide-review-ui-tests.log` |
| `npm run build` | TypeScript 和 Vite 构建通过；`guide-review-build.log` |
| 真实批次核心＋假 Provider | 完整正文、一次结构修复、一次覆盖补充、检查点重放后调用数不增加 |
| 真实 memo＋临时数据库＋假 Provider | 只从 PDF 根分支调用、Reader 不混入根、一次修复、原文恢复、缓存命中、收据不重复 |
| 发布恢复 | 旧 publishId 在后续 head 已更新时不能回滚当前结果 |
| 配置与资源 | 并发提交、中断事务、备份恢复、目录保留、移除/替换头像、工作区搬迁、路径边界 |

生产构建仍有既有的大 chunk 提示，不影响本次编译通过。没有执行真实 Provider、自动生成用户论文旁批、提交 git 或发布应用。

## 实现差异与仍待验收

- **配置位置差异**：另一位 AI 已将原计划的应用配置目录单文件改为工作区 `Characters/`。本轮沿用并加固该布局，明确记录在 [批注角色合同](guide-characters.md)，未把原计划路径悄悄改写成已达成。
- **P7 未完成**：真人对口吻亲切程度、密度、事实准确性、阅读干扰的评价，以及真实桌面视觉验收。假模型测试只验证工程流程，不代表内容质量已验收。
- 早期不完整的 V2 排队任务缺少冻结 PDF／批次／根提示词时，会在继续调用模型前明确要求重新计划。历史 V1 不强制转为 V2。
- 自动审批曾拒绝大范围接入命令；这些命令未执行。用户继续后改为独立模块编译、单个命令接入、memo 接入、批次接入逐段验证，所列接入现已完成，无待批准的本轮代码操作。

Standards：5 组数据与兼容问题已处理；Spec：上轮 7 项主问题已处理。两轴均无本轮已确认但未修复的运行时阻断；内容与桌面视觉验收单列保留。
